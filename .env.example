# ==========================================
# PAYLOAD CMS CONFIGURATION (Required)
# ==========================================

# PAYLOAD_SECRET - Secret key for Payload CMS authentication and encryption
# ⚠️  CRITICAL: Must be at least 32 characters long and unique per environment
# Generate a secure key: openssl rand -base64 32
# Production: Use a strong, randomly generated secret stored securely
# Development: Can use a simpler key, but still keep it private
PAYLOAD_SECRET=your_secret_key_here_32_characters_min

# MONGODB_URI - MongoDB database connection string
# ⚠️  REQUIRED: Server will fail to start without this
# Local Development: mongodb://localhost:27017/portfolio
# Docker: mongodb://mongo:27017/portfolio
# MongoDB Atlas (Cloud): mongodb+srv://username:password@cluster.mongodb.net/portfolio?retryWrites=true&w=majority
# Note: Ensure database user has read/write permissions
MONGODB_URI=mongodb://localhost:27017/portfolio

# ==========================================
# SERVER CONFIGURATION
# ==========================================

# PAYLOAD_PUBLIC_SERVER_URL - Public URL where Payload CMS is accessible
# Used for: CORS, CSRF protection, webhooks, and email templates
# Development: http://localhost:3001
# Production: https://cms.yourdomain.com or https://yourdomain.com/cms
PAYLOAD_PUBLIC_SERVER_URL=http://localhost:3001

# PORT - Port number for the Payload CMS backend server
# Default: 3001 (if not specified)
# Production: Often set by hosting provider (e.g., Railway, Heroku)
PORT=3001

# ==========================================
# FRONTEND CONFIGURATION
# ==========================================

# REACT_APP_API_URL - Backend API endpoint for React app to fetch CMS data
# Must point to the /api route of your Payload server
# Development: http://localhost:3001/api
# Production: https://cms.yourdomain.com/api
REACT_APP_API_URL=http://localhost:3001/api

# REACT_APP_URL - Frontend application URL
# Used for: CORS whitelist configuration
# Development: http://localhost:3000
# Production: https://yourdomain.com or https://www.yourdomain.com
REACT_APP_URL=http://localhost:3000

# ==========================================
# EMAILJS CONFIGURATION (Optional)
# ==========================================
# Used for contact form functionality
# Sign up at: https://www.emailjs.com/
# Replace with your EmailJS credentials from dashboard

# REACT_APP_EMAILJS_SERVICE_ID - Your EmailJS service ID
REACT_APP_EMAILJS_SERVICE_ID=service_2fil88f

# REACT_APP_EMAILJS_TEMPLATE_ID - Your EmailJS template ID
REACT_APP_EMAILJS_TEMPLATE_ID=template_ksmke1h

# REACT_APP_EMAILJS_PUBLIC_KEY - Your EmailJS public key (formerly user ID)
REACT_APP_EMAILJS_PUBLIC_KEY=h9e4_z0Y1zbBb54me

# ==========================================
# BUILD & OPTIMIZATION (Optional)
# ==========================================

# GENERATE_SOURCEMAP - Generate source maps for debugging
# Development: true (easier debugging)
# Production: false (smaller bundle size, better security)
GENERATE_SOURCEMAP=false

# DISABLE_ESLINT_PLUGIN - Disable ESLint during builds
# Set to true to speed up builds if linting is done separately
DISABLE_ESLINT_PLUGIN=true

# ==========================================
# ANALYTICS & MONITORING (Optional)
# ==========================================

# Uncomment and configure if using analytics/monitoring services

# REACT_APP_SENTRY_DSN - Sentry error tracking DSN
# Sign up at: https://sentry.io/
# REACT_APP_SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id

# REACT_APP_GA_TRACKING_ID - Google Analytics tracking ID
# Format: G-XXXXXXXXXX (GA4) or UA-XXXXXXXXX (Universal Analytics)
# REACT_APP_GA_TRACKING_ID=G-XXXXXXXXXX

# ==========================================
# NOTES FOR DEPLOYMENT
# ==========================================
#
# 1. Never commit .env files to version control
#    - Always use .env.example as a template
#    - Store actual secrets in hosting platform's environment variables
#
# 2. Required variables for production:
#    - PAYLOAD_SECRET (unique, 32+ chars)
#    - MONGODB_URI (valid connection string)
#    - PAYLOAD_PUBLIC_SERVER_URL (your domain)
#    - REACT_APP_API_URL (your API endpoint)
#
# 3. Environment-specific .env files:
#    - .env.development (local dev)
#    - .env.production (production build)
#    - Set via hosting platform for deployed environments
#
# 4. Security best practices:
#    - Rotate PAYLOAD_SECRET periodically
#    - Use MongoDB user with minimal required permissions
#    - Enable MongoDB authentication in production
#    - Use HTTPS in production (never HTTP)
#    - Keep dependencies updated
#
# ==========================================
